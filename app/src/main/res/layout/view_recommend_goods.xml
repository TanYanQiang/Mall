<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:layout_marginRight="10dp"
    android:orientation="vertical">

    <ImageView
        android:id="@+id/goodsThumb"
        android:layout_width="@dimen/order_goods_thumb_width"
        android:layout_height="@dimen/order_goods_thumb_width"
        android:scaleType="centerCrop"
        android:src="@mipmap/ic_launcher" />

    <TextView
        android:id="@+id/goodsPrice"
        android:layout_width="wrap_content"
        android:layout_height="24dp"
        android:layout_alignBottom="@id/goodsThumb"
        android:layout_alignLeft="@id/goodsThumb"
        android:layout_alignRight="@id/goodsThumb"
        android:background="@drawable/recommend_goods_text_bg"
        android:gravity="center"
        android:text="￥110.8"
        android:textColor="@color/white"
        android:textSize="12sp" />
</RelativeLayout>